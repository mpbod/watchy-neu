cmake_minimum_required(VERSION 3.16)
get_filename_component(WATCHY_ROOT ${CMAKE_CURRENT_LIST_DIR}/../../.. ABSOLUTE)
set(EXTRA_COMPONENT_DIRS "${WATCHY_ROOT}/managed_components")
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(grid01)
include(elf_loader)
include(${WATCHY_ROOT}/sdk/cmake/WatchyPackage.cmake)
watchy_project_so(grid01)
